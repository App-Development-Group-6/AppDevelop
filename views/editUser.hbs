<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Edit User</title>
</head>
<body>
    <div class="navbar navbar-dark bg-dark box-shadow">
        <div class="container d-flex justify-content-between">
          <a href="/" class="navbar-brand d-flex align-items-center">
            <strong>FPT Software</strong>
          </a>
        </div>
    </div>
    <h2 class="mt-5 mb-5 text-center text-success"> Update User</h2>
    <div class="col-md-6 offset-md-3">
        <form action="/update" method="post">
            <input type="hidden" name="txtId" value="{{Users._id}}">
            Username : <input type="text" class="form-control" name="txtUname" value="{{user.name}}">
            <Br>
            Fullname : <input type="text" class="form-control" name="txtFullname" value="{{user.fullname}}">
            <br/>
            Password : <input type="password" class="form-control" name="txtPassword" value="{{user.password}}">
            <br/>
            Role :
                <select name="txtRole" class="form-control" value="{{user.role}}">
                    <option>Admin</option>
                    <option>Staff</option>
                    <option>Trainer</option>
                    <option>Trainee</option>
                </select>
            <Br>
            Age : <input type="number" class="form-control" name="txtAge" value="{{user.age}}">
            <Br>
            Gender :
                <select name="txtGender" class="form-control" value="{{user.gender}}">
                    <option>Male</option>
                    <option>Female</option>
                </select>
            <Br>
            Number : <input type="number" class="form-control" name="txtNumber" value="{{user.number}}">
            <Br>
            Email : <input type="email" class="form-control" name="txtEmail" value="{{user.email}}">
            <Br>
            <input type="submit" class="btn btn-info mt-2" value="Update">
        </form>
    </div>
</body>
</html>
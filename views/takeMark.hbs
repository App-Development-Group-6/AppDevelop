<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="mystyle.css">
    </link>
    <title>Take Mark Page</title>
</head>

<body>
    <center>
        <div class="navbar navbar-dark bg-dark box-shadow">
            <div class="container d-flex justify-content-between nav-menu">
                <a href="/" class="navbar-brand d-flex align-items-center">
                    <img src="images/fpt.png" alt="FPT Brand" width="30px" height="30px">
                    <strong>FPT Software</strong>
                </a>
                <a a href="/trainerIndex" class="navbar-brand d-flex align-items-center">
                    <strong>Home</strong>
                </a>
                <a href="/trainer/course" class="navbar-brand d-flex align-items-center">
                    <strong>Course</strong>
                </a>
                <a href="/profile" class="navbar-brand d-flex align-items-center">
                    <strong>Profile</strong>
                </a>
                <a href="/trainer/about" class="navbar-brand d-flex align-items-center">
                    <strong>About</strong>
                </a>
                <a class="text-decoration-none account_image " href="/profile"><img class="" src="images/account.png"
                        alt="account icon" width="25px" height="25px">{{dataInfo.role}} - {{dataInfo.userName}}</a>
                <a class="navbar-brand d-flex align-items-center btn btn-danger" href="/logout"><strong>
                        Logout</strong></a>
            </div>
        </div>
        <br>
        <div>
            <h1>Take mark for Trainee: </h1>
            {{#if data.grade}}
            <h2>{{data.userId}} - {{data.name}} with Current mark: <i style="color:darkmagenta">{{data.grade}}</i> </h2>
            {{else}}
            <h2>{{data.userId}} - {{data.name}} with Current mark: None </h2>
            {{/if}}
            <br>
            <div>
                <form action="updateMark" border="1" method="post">
                    <input type="hidden" name="id" value="{{data._id}}" id="">
                    <input type="hidden" name="courseId" value="{{data.courseId}}" id="">
                    <h2>Take (new) grade:</h2>
                    <select name="txtGrade" id="" value="{{data.grade}} ">
                        <option class="alert alert-danger">Passed</option>
                        <option class="alert alert-danger">Failed</option>
                    </select> <input class="btn btn-success" type="submit" value="Grade">
                </form>
            </div>
        </div>
    </center>
</body>

</html>